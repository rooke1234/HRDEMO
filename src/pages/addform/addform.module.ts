import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddformPage } from './addform';

@NgModule({
  declarations: [
    AddformPage,
  ],
  imports: [
    IonicPageModule.forChild(AddformPage),
  ],
})
export class AddformPageModule {}
